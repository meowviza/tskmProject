@model IEnumerable<tskmProject.Models.Ticket>

@{
    ViewBag.Title = "รายการใบงาน";
}

<div class="row">
    <h3 class="page-header">รายการใบงาน<</h3>
</div>

<button type="button" class="btn btn-default">สร้างใบงาน</button>

<br />
<br />

        <table class="table table-striped">
    <tr>
        <th>รหัสใบงาน</th>
        <th>ชื่อใบงาน</th>
        <th>ประเภทใบงาน</th>
        <th>ผู้รับผิดชอบ</th>
        <th>วันที่สร้างใบงาน</th>
        <th>สถานะใบงาน</th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                test
            </td>
            <td>
                test
            </td>
            <td>
                test
            </td>
            <td>
                test
            </td>
            <td>
                test
            </td>
            <td>
                test
            </td>
            <td>
                @Html.ActionLink("แก้ไข", "Edit", new { id = item.ticketID }) |
                @Html.ActionLink("แสดงรายละเอียด", "Details", new { id = item.ticketID }) |
                @Html.ActionLink("ลบ", "Delete", new { id = item.ticketID })
            </td>
        </tr>
    }

</table>

